<%= form_with(model: @room, remote: true) do |f| %>
  <%= f.text_field :name, class: "form-control mb-2 shadow-lg  ", autocomplete: 'off' %>
  <%= f.submit("Створити кімнату",data: { disable_with: false },class: "btn btn-primary mb-1 shadow-lg")%>
<% end %>